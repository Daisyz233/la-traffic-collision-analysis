# Scripts

This folder contains reusable Python scripts for the project.

Current file:

- `download_data.py`: downloads the raw LA Open Data traffic collision dataset as a CSV.

Future scripts can include:

```text
clean_data.py
feature_engineering.py
train_random_forest.py
create_visualizations.py
```
